public class ProblemaDoDiamante {
    /*
     * A herança múltipla é uma forma de reutilização de código que permite que uma classe herde de mais de uma classe.
     * 
     * Em Java, a herança múltipla não é permitida. Isso porque, ao herdar de duas classes, se houver um método com a mesma assinatura em ambas as classes, o compilador não saberá qual método chamar.
     * 
     * Para contornar esse problema, Java utiliza interfaces. Uma interface é um contrato que uma classe concreta deve cumprir. Dessa forma, uma classe pode implementar várias interfaces, mas herdar de apenas uma classe.
     */
}
