public class Comparable {
    /*
     * Interface: 
     * 
     * public interface Comparable<T> {
     *  int compareTo(T o);
     * 
     * Com "o" sendo um objeto. É como se fosse uma lista, do Tipo T.
     * 
     * Problema motivador
     * 
     * Faça um programa para ler um arquivo contendo nomes de pessoas, armazenando-os em uma lista. 
     * Depois de ordenar os dados dessa linha e mostra-los ordenadamente na tela. Nota: o caminho do arquivo 
     * pode ser informado pelo usuário.
     * }
     */
}
