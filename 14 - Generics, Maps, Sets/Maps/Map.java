public class Map {
    /*
    É genérico, mas Map<K, V>, isto é, tem um tipo chave e um tipo valor.

    É uma coleção de pares chave/valor
    * Não admite repetições no objeto chave.
    * Os elementos são indexados pelo objeto chave.
    * Acesso, inserção e remoção de elementos são rápidos

    * Principais implementações:
    1. HashMap
    2. TreeMap
    3. LinkedHashMap

    * Métodos importantes
    1. put(key, value), remove(key), containsKey(key), get(key)

    2. clear()

    3. size()

    4. keySet() - Set<K>
    5. values() - Collection<V>

    Novamente, se não há uma função @Override que sobrescreva o método hashCode e equals, irá
    ser feita a comparação de ponteiros.

     */
}