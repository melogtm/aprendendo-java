public class InterfacesFuncionais {
    /*
    É uma interface que possui um único método abstrado. Suas implementações
    serão tratadas como expressões lambdas.

    Aquilo do MyComparator sendo uma clsse com a implementação compare

    Em Main, fica
    list.sort(new MyComparator());

    A expressão lambda é tratada como uma interface para o Java.
     */
}
